name: "Update Changelog"

on:
  release:
    types: [ released ]

permissions: {}

jobs:
  update:
    permissions:
      contents: write
    uses: simdata-sosial/.github/.github/workflows/update-changelog.yml@main
